# failure module

Benchmarks are run via `Main.scala` and the functions:
- `run()`: Used to create box plot data with steps to reach coverage/completion.
- `run_coverage()`: Used to create data for coverage over time.

The benchmark programs (as well as the implementation for scheduling events) are found in `Benchmarks.scala`.
